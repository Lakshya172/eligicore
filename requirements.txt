# EligiCore — Week 1 dependencies
#
# Only what Week 1 actually needs. The rest of the dossier's stack (pdfplumber,
# python-docx, spaCy, scikit-learn, openpyxl) arrives with the phases that use it.
# Versions are pinned so builds are reproducible (standards/security_privacy.md §6).

# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0

# --- Validation and configuration ---
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0
python-dotenv==1.0.1

# --- Operational database ---
SQLAlchemy==2.0.36
alembic==1.14.0

# --- Testing ---
pytest==8.3.4
httpx==0.28.1
