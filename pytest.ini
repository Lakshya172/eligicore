[pytest]
# `pythonpath = .` puts the project root on sys.path so tests can `import app`
# without the package being installed.
pythonpath = .
testpaths = tests
python_files = test_*.py
addopts = -ra --strict-markers
